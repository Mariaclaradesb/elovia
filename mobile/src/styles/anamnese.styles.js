import { colors } from '../theme/colors';

export const anamneseStyles = {
  anamneseAccordionContent: {
    gap: 10,
    paddingBottom: 14,
    paddingHorizontal: 14,
  },
  anamneseSearchResult: {
    backgroundColor: colors.white,
    borderColor: colors.line,
    borderRadius: 16,
    borderWidth: 1,
    gap: 5,
    padding: 12,
  },
};
